<!DOCTYPE html>
<html lang="en">
<head>
    <title>Blogging App</title>
    <%- include('./partials/head') %>

</head>
<body>
    <%- include('./partials/navbar') %>
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-10 offset-md-1">
                <div class="card">
                    <div class="d-flex justify-content-end mr-5 mt-2 mb-0">
                        <% if (locals.user._id == blog.createdBy) { %>
                            <a href="/blog/edit/<%= blog.slug %>" target="_blank" class="card-link text-success">edit</a>
                            <form action="/blog/delete/<%= blog.slug %>" method="post">
                                <button class="btn btn-outline-none text-danger p-0 pl-4" type="submit"
                                onclick="return confirm('Are you sure you want to delete this blog?')" >delete</button>
                            </form>
                        <% } %>
                    </div>
                    <img src="../public/images/blog-images/<%= blog.displayImage %>" class="card-img-top px-5 pt-5" alt="<%= blog.title %>">
                    <div class="card-body px-5">
                        <h5 class="card-title"><%= blog.title %></h5>
                        <p class="card-text"><small class="text-muted">Published on <%= new Date(blog.createdAt).toLocaleDateString() %></small></p>
                        <p class="card-text text-justify"><%= blog.content %></p>
                    </div>
                </div>
            </div>
        </div>
    </div>    
    <%- include('./partials/script') %>
</body>
</html>